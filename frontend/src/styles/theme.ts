import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ff3074",
    },
    background: {
      paper: "#FFF",
      default: "#e0e1dd",
    },
  },
});

export default theme;
